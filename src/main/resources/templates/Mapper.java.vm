package ${package}.mapper;

import ${package}.common.minemappers.MyMapper;
import ${package}.entity.${className};

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends MyMapper<${className}> {
}